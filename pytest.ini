[pytest]
testpaths = tests
markers =
    smoke: Quick interface presence checks.
    unit: Unit-level tests.
    integration: Integration-level tests.
    placeholder: Placeholder scaffolding verification.
    perf: Performance benchmark/regression tests.
    soak: Long-running soak tests.
    e2e_large: End-to-end large dataset tests.

