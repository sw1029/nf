> 구현 순서(Phase) 통제: `plan/IMPLEMENTATION_CHECKLIST.md`

<<목적>>
1. 사용자가 별도로 설정, 플롯, 등장인물 문서 작성(설정 문서에 별도 태깅, 부연 설명 등으로 부가정보, 분류, 계층적 분류 제공.)

2. 작성한 문서 인덱싱 or 임베딩(RAG 목적)

3. 사용자가 프로그램 내에서 작성중인 txt의 정합성 위배 부분 txt segmentation(해당 내용은 별도의 신뢰성 점수를 매겨 사용자에게 제공하며, 사용자는 필요 시 해당 부분에서 같은 제안을 반복하지 않도록 해당 문장만 예외처리 가능. 혹은 문장 비교가 아닌 팩트/이벤트 기반 제약 검사로 구현하는 것도 고려중. 목표는 사용자가 작성 중인 글에서 어떤 부분이 정합성이 위배되었는지만 확인 가능하면 됨. 제약은 두 단계로 나누어 segmentation. (High precision, Low recall) 부분은 스키마의 “명시 필드”만 사용->나이/시간/장소/관계/사망 여부 등. 위배 시 근거는 스키마 경로 + 원문 근거(FTS)로 제시. (Heuristic/Model, Unknown 허용)-> “암시/추정/심리/서술 트릭” 영역은 unknown으로 두고, 근거 충돌 시 보류. 이 레이어만 작은 모델(NLI/분류기) 또는 API에 위임. 신뢰성 점수는 점수의 근거를 FTS 매칭 강도/근거/확정근거/모델 추론 점수 로 분해하여 제공). 정합성 체크 단계에서 의도된 모순/거짓말은 처음엔 정합성 오류 형식으로 띄우되, 사용자가 의도한 부분이라고 체크 시 화이트리스트 처리 + 추가 정보 형식으로 저장. 정합성의 기준은 사실/시간 등의 하드 제약을 우선으로 하며 서술 트릭/거짓말 등의 요소는 앞서 말한 화이트리스트의 방식을 따름

4. 문법 교정(사용자 요청 강도에 따라 조절)

5. 자간, 줄 간격 설정(에디터 UX) 제공(레이아웃 설정)

6. 사용자가 요청 시 위의 내용 바탕으로 개선 제안(bitNet, 혹은 Gemma 등을 양자화한 LLM 혹은 api wrapper. 문장, 문학적 요소는 최대한 사용자가 사전 지정한 내용을 따라 RAG 내용 바탕으로 하여 기존 rule base 기반 교정 필요사항 등에 대한 내용을 요약 정리. 제안 시에는 결과를 근거 인용 형식으로 어떤 문서와의 정합성이 어긋나는지 보여줄 수 있어야 함(인용은 “RAG 상위 k chunk”를 그대로 붙이는 게 아니라 문서ID/섹션/태그 경로(예: 설정/인물/주인공/나이)까지 보여주는 방식. 이를 위하여 사용자 작성 문서를 별도로 schema 형식으로 변환하는 과정 또한 구현되어야 함) + 설정 문서 및 기존 스토리 내용 기반 설정 정합성 분석 시에는 필요 시 gpt api 호출 등의 방식으로 별도 구현. 해당 내용은 별도의 신뢰성 점수를 매겨 사용자에게 제공하며, 사용자는 필요 시 해당 부분에서 같은 제안을 반복하지 않도록 해당 문장만 예외처리 가능. 혹은 문장 비교가 아닌 팩트/이벤트 기반 제약 검사로 구현하는 것도 고려중)

7. 작성한 글을 txt, docx등으로 export 기능 제공

8. 소설의 n화 등을 n~m화 구간 지정을 통한 episode chunk 구성 제공(RAG 목적 + 기승전결 분석(이 기능은 optional하게 여유가 된다면 구현. 에피소드별 사건 밀도/등장인물 분포/떡밥 태그 변화량 같은 계량 지표 형식으로 우선 표기))

8-1. 추가 요구: episode chunk 뿐만 아니라 **시점(time_key)**, **인물(entity_id)** 기준으로도 chunk group을 구성하여
RAG/정합성/제안에서 필터링 가능한 형태로 확장.
  - 생성은 “항상 자동”이 아니라 **사용자 요청 시에만** 실행(리소스/오염 방지).
  - 1차 제안(초기값; 보수적):
    - 시점: **상대 시간**을 우선 사용하고, **화수/에피소드 범위 기준으로 1차 매핑**(예: episode range + scene index + rel key).
    - 인물: 사용자 요청 시에만 “어느 문장~어느 문장” 정도의 **span 범위**를 `entity_id`에 링크(mention span 스타일).
  - 사용자 검토/승인 후 필요 시 2차 제안(정교화/재매핑/링크 인덱스 생성 등)을 수행.

8-2. 추가 요구: **세계관 타임라인 문서**를 별도로 두고, 지속적으로 참조/확장 가능해야 함.
  - 상대 서술 시점을 우선으로 하되, 사용자 검토를 통해 타임라인과의 매핑을 조정 가능.
  - “시점 chunk/time anchor”는 타임라인을 참조하여 **timeline_idx(순서/인덱스)** 를 가질 수 있어야 함.

9. 디바이스 한계를 우선 고려하여 rule base로 구현 가능한 경우는 사용자 지정(드래그 후 해당 문장에 태깅(복선과 같은 태그)와 같이 사용자 참여 기반 지정을 통한 rule base 병행 고려. 필요 시 태깅 품질을 판단하는 간단한 로컬 ai 추가(일관성, 지나치게 희소한 태그 여부 등을 기반으로 하는 구현 단계에서 사용자 정의 고차 feature를 통한 regression 형식 고려중. 초기에 데이터가 많이 쌓이지 않은 경우 휴리스틱/혹은 데이터가 일정 이상 쌓일 때까지 보류))

10. 고용량 모델은 선택적으로 다운. 초기에 다운 여부를 묻고 아니라면 gpt, gemini api key를 받는 등의 형태로 구현

11. '정합성 검토'용 소형 모델과 '문장 생성/개선'용 고성능 API 모델을 분리. 두 모델의 역할은 명확히 분리됨.

12. 정합성 검토를 3단으로 강제 근거화 후 모르면 모른다는 형식으로 별도 처리



실제 배포하여 사용자 디바이스 한계(평범한 일반 사용자용 데스크탑. windows 우선 고려)를 고려하여 최대한 최적화 진행을 목표로 함.

python 프로젝트로 구현 후 exe 형식으로 실행파일 형태로 제공 목표. 가능한 한 로컬 디바이스에서 구동 가능하도록 하며, 특정 기능만 필요 시 api 호출 방식으로 성능이 좋은 llm을 호출하여 기존의 로컬 모델보다 나은 답변을 얻을 수 있도록 구성.

문학적 작성/고난도 판단은 필요 시 api 호출 후 위임 가능성도 고려중.


실제 구현 시 각 기능과 하위 기능을 모듈화 -> 기능/책임 분산시켜 구현 예정

앱/모델/인덱서 분리 + 디스크 기반 FTS/벡터 + 백그라운드 작업 큐 + 메모리 상한 적용 예정.


<<예상 사용 기술(추가, 수정, 삭제 가능)>>
- Gemma 등의 모델 양자화
- Graph 기반 RAG(계층적 indexing도 병행 고려. 가능하다면 사용자 지정 태그 기반 연산 간소화도 같이 고려. 가능하다면 RAPTOR와 같은 트리 구조 인덱싱도 병행 고려. 화당 5000자 * 1000화가 넘는 경우에서의 RAG 적용 가능성 여부도 병행 검토 예정)
- Graph 기반 RAG와 더불어 optional하게 하이브리드 검색 + 얕은(가벼운) 그래프/제약 엔진 분기도 같이 구현(사용자 선택으로 필요 시 성능 향상시키는 형식). 필요시 해당 기능을 우선 구현 후 GraphRAG/RAPTOR는 실험적 기능으로 분리.
- ONNX
- FAISS, vector DB
- semantic chunking
- 태깅(우선 정적으로 개발자 정의 태그를 제공. 필요 시 사용자 정의 태그 형식으로 확장. 사용자가 태그 미정 시 자동 태깅 제공(간단한 소형 모델 기반))
- BM25/FTS (RAG 인용 목적)
- 정보 자체를 datacard로 취급하여 특정 json 혹은 dataframe, 혹은 처리가 용이한 데이터 확장자로 정량화. 같은 schema를 공유하도록 구현
- schema에서의 설계 실패/판단 실패의 오염을 막기 위한 gating(타입/단위/정규화, 근거, 중복/동일성(identity), 제약/규칙, 충돌 감지, Model/Heuristic(추정을 통한 오염 방지), query time, schema 버전). 저장소 등급 분리 및 충돌 시 unknown 강등 등 적용
- 가능하면 rag indexing 등 초기 리소스 투자가 더 필요한 부분은 백그라운드 실행, 프로세스를 세분화하여 최대한 디바이스 부담을 줄임


<<2차 사용 기술 후보군>>
- HNSWlib


<<잘 모르지만 AI에게 제안받은 기술>>
- Self-Rag & Verifiable Fact extraction via Triplet -> 정합성 검토
	: 그렇다면 fact 추출 자체는 어떻게 진행하여야 하는가의 문제가 존재
	->  추출 결과는 제안, 저장은 사용자 승인(이 부분은 자동 추출 결과를 chatbot이 정보를 wrapping하는 형식으로 자연어 형태로 부드럽게 제안. 가능하다면 팩트/이벤트 추출을 ‘자동 확정’이 아니라 ‘근거 있는 제안’으로 제한)

해결하고자 하는 추가 문제점
1. 사용자가 1000화 이상의 원고를 한꺼번에 분석 요청할 경우, Vector DB의 인덱스가 메모리를 점유하고 LLM 추론이 겹치면서 시스템 전체가 프리징될 가능성 -> 프로세스 분리, 디스크 기반 indexing/배치 빌드, 2단 검색, 전역 토큰(세마포어) 적용(동시작업 방지). 인덱서/임베더를 별도 프로세스로 돌리고, 죽여도 UI가 살도록. 대형 벡터 인덱스는 항상 RAM에 올리는게 아닌 필요한 shard만 로드. 모델/인덱스는 샤드 단위 로드 + 작업 시점 로드/언로드
2. Python 기반의 EXE 파일은 용량이 비대하며(최소 수 GB), 로컬 환경의 CUDA 버전, C++ 재배포 패키지 등 종속성 문제 -> 앱/모델 분리 다운로드, 모델 런타임/인덱서(FTS/벡터) 분리
3. '신뢰성 점수'를 제공하더라도 모델 자체가 확신에 찬 환각(Hallucination)을 보일 경우, 사용자는 시스템을 신뢰하지 못하게 되는 경우 -> 근거 강제, 근거 충돌 시 불확실로 강등, 다중 근거 병합



최종 고려 방향

문서→스키마(태그/속성) 기반 근거화”를 최우선
검색은 “BM25(정확 인용) → 벡터(의미 확장)”의 2단으로 단순화
정합성은 “클레임-근거-판정(unknown 허용)” 구조
GraphRAG/RAPTOR/SELF-RAG/BitNet은 기본 골조 구현 후 optional하게 구현을 얹는 방향으로(구현된 모듈에서 optional하게 선택 가능하게)
근거 표현을 문서ID/섹션/태그 경로 + FTS 스니펫 등의 규격화된 방식으로 표준화
신뢰성 “단일 점수”를 초기에 강하게 전면 노출, 자동 추출 결과를 high-precision 스키마에 자동 반영 하는 것은 지양
